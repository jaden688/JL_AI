# Secrets — never commit the API key in any form
.env
.env.*
*.known-good-backup

# Source backups created during edits (local only)
*.backup-before-port

# Database
*.db
*.sqlite
!sparkbyte_memory.db
!data/sparkbyte_memory.db
!data/a2a_accounts.db
!data/a2a_usage.db

# Julia artifacts
Manifest.toml
*.jl.cov
*.jl.*.cov
*.jl.mem

# Temp files from execute_code tool
tmp_exec_*.py
tmp_exec_*.jl

# Telemetry & logs
full_telemetry.jsonl
telemetry_logs/
*.jsonl
*.log
thought_log.txt
telemetry_test.txt

# Claude Code local config / memory
.claude/

# Dynamic tool registry (runtime-generated)
dynamic_tools_registry.json
dynamic_tools.jl

# Inspection / scratch dirs
_mcp_inspect/
mcp.zip
a2a-inspector/
a2a-tck/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.CondaPkg/

# OS
.DS_Store
Thumbs.db
pitreader_backup_copy/

# Nested engine variants (separate repos)
src/j-engine/
src/jl-engine-local/
